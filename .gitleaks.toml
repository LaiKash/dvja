# Gitleaks configuration for DVJA
#
# Generic password/keyword detection is handled by detect-secrets
# (KeywordDetector + HexHighEntropyString plugins).
# Gitleaks focuses on provider-specific tokens using its default ruleset.

title = "DVJA Gitleaks Configuration"

[allowlist]
paths = [
  '''target/''',
  '''\.idea/''',
  '''node_modules/'''
]
